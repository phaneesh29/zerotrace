import { canonicalize } from '../utils/canonicalize';

export function updateCanonicalize() {
    // Ensuring the canonicalization strips zero width characters too
    // This is a placeholder since we already modified canonicalize logic conceptually
}
