/**
 * Shared configuration for the web app (app/server) and the core
 * watermarking logic in src/.
 */

export const KEY_FILE = 'demo-keys.json';
export const SECRET_KEY = 'zerotrace-secret-research-key';
